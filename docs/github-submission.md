# GitHub submission

Repository: https://github.com/codersusu/game-cinematic-trailor

Branch: `main`

Verified commit: `e46461a65e853e456995a0225bae89a30959b173`

The persistent local Git checkout is `github-submission/` beside this document's parent directory. It contains the curated Arrival submission, portable launch/render instructions, required scene assets, soundtrack, credits, and both finished movies. The original production workspace remains intact.

All 79 Git LFS objects uploaded successfully (547 MB). GitHub's main branch was verified against the local commit, and the public Arrival movie URL returned HTTP 200 with the expected file size. The staged Blender scene passed its dependency audit with no missing images or references outside the repository. No credential files or personal machine paths were included in the submitted text files.

The original v1 editable scene, bundled applications, generated frame sequences and temporary backups remain local and are not in the repository. Use `git lfs pull` after cloning to retrieve the binary assets.
